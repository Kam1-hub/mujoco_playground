# Known Issues

## Initial Known Issues

- Runtime dependencies were not installed in the preparation environment.
- `template` was copied as a new repo; no upstream remote is configured.
- Route A depends on installed Brax SAC API availability.
- `truncation` runtime presence still needs Phase 1 validation.
- G1 action scaling is internal to env step; SAC must avoid double-scaling.

## LIFT Traps To Avoid

See copied report:

```text
external_references/LIFT-humanoid-reports/bug_traps.md
```

