# Bug / Trap Candidates

| Issue | Severity | Evidence | Why it matters | Patch sketch |
|---|---:|---|---|---|
| Main `--learning_rate` does not affect SAC actor/critic/alpha LR | High | CLI flag exists (`train_in_mujoco_playground.py:113`), but main script writes `sac_params.learning_rate` (`train_in_mujoco_playground.py:191`, `train_in_mujoco_playground.py:192`) while train function expects `alpha_learning_rate`, `actor_learning_rate`, `critic_learning_rate` (`policy_pretrain/train.py:149`, `policy_pretrain/train.py:151`). | User may think LR is changed while learner silently keeps config defaults; allowed-arg filtering can drop unknown key. | Replace one flag with three explicit flags or map `learning_rate` to all three fields, as Optuna runner does (`train_in_mujoco_playground_optuna.py:292`, `train_in_mujoco_playground_optuna.py:294`). |
| Main `--value_hidden_layer_sizes` writes wrong network key | High | Config expects `q_hidden_layer_sizes` (`lift_configs.py:27`); network factory expects `q_hidden_layer_sizes` (`policy_pretrain/sac_networks.py:108`); main CLI writes `value_hidden_layer_sizes` (`train_in_mujoco_playground.py:205`). | Q-network size override likely does not take effect in main runner. | Rename CLI destination to `network_factory.q_hidden_layer_sizes`; keep alias for backward compatibility. |
| CLI default `value_obs_key` is `state`, while config default is `privileged_state` | Medium | CLI default is `"state"` (`train_in_mujoco_playground.py:133`); config uses `"privileged_state"` (`lift_configs.py:29`). | If flag presence semantics or wrapper changes cause override, asymmetric critic can be disabled unintentionally. | Make CLI default `None` and only override config when user explicitly passes it; or set default to `privileged_state`. |
| Vision path is declared but unsupported | High | `_VISION` flag exists (`train_in_mujoco_playground.py:71`); using it raises `ValueError("not implement")` (`train_in_mujoco_playground.py:218`, `train_in_mujoco_playground.py:219`). | Embodied AI/vision experiments cannot use this SAC runner as-is. | Hide flag or implement vision wrappers, image observation keys, CNN policy, and replay handling. |
| Hardcoded observation keys | High | SAC dummy obs uses `state`, `privileged_state`, optional `wm_state` (`policy_pretrain/train.py:273`, `policy_pretrain/train.py:277`, `policy_pretrain/train.py:282`). | New MuJoCo Playground envs with different dict keys will fail or train wrong networks. | Add observation-schema adapter or infer keys from config and `observation_size`. |
| `robot_config` is required by SAC loss but may be `None` | High | Main resolver returns `None` if env name lacks `g1` or `t1` (`train_in_mujoco_playground.py:154`, `train_in_mujoco_playground.py:162`); loss uses `robot_config.policy_output_scale` unguarded (`policy_pretrain/losses.py:43`). | Non-G1/T1 tasks can fail at loss construction. | Provide identity scale fallback when `robot_config is None`; require explicit action scale config. |
| Local forks can shadow upstream packages | High | Playground wheel packages `mujoco_playground` (`mujoco_playground/pyproject.toml:66`, `mujoco_playground/pyproject.toml:67`); `brax_env` uses `find_packages()` and contains/imports `brax` (`brax_env/setup.py:31`, `brax_env/brax_env_utils.py:20`). | Installing editable local forks can override upstream `mujoco_playground` or `brax` imports. | Keep isolated venv; do not install into main pipeline env until package ownership is decided. |
| `mujoco_playground` comments out Brax dependency | Medium | Dependency line is commented (`mujoco_playground/pyproject.toml:26`) while wrappers/configs depend on Brax-style code. | Fresh installs may miss Brax-compatible APIs. | Pin known-compatible Brax or keep using local `brax_env`. |
| Gymnasium mismatch | Medium | `requirements.txt` has Gymnasium (`requirements.txt:1`), but wrapper imports legacy Gym (`brax_env/brax/envs/wrappers/gym.py:20`, `brax_env/brax/envs/wrappers/gym.py:27`). | Gymnasium users may hit API differences. | Add a Gymnasium-specific adapter; keep legacy Gym wrapper separate. |
| World-model pretrain can wait forever for pickle | High | Infinite loop waits for `epoch_mini_epoch.pkl` and sleeps if missing (`world_model/pretrain_wm.py:502`, `world_model/pretrain_wm.py:507`). | Missing/renamed buffer shard causes silent hang. | Add timeout, max shard count, and explicit missing-data error. |
| `train_wm_from_file.py` docstring says PPO | Low | Docstring: `"Train a PPO agent..."` (`train_wm_from_file.py:15`); code calls WM train on SAC buffer data (`train_wm_from_file.py:196`, `train_wm_from_file.py:201`). | Misleads auditors/users. | Fix docstring to WM pretraining. |
| Fine-tune env names are hardcoded | High | `finetune.py` env dict is literal (`finetune.py:251`, `finetune.py:258`); WM pretrain env dict is literal (`train_wm_from_file.py:142`, `train_wm_from_file.py:158`). | New envs require code edits, not config/registry entries. | Replace with registry/factory adapter. |
| Fine-tune policy factory ignores configurable obs keys | Medium | Fine-tune factory passes hidden sizes and dtypes but not `policy_obs_key`/`value_obs_key` (`finetune.py:312`, `finetune.py:320`); SAC factory has these args (`policy_pretrain/sac_networks.py:116`, `policy_pretrain/sac_networks.py:117`). | General asymmetric layouts beyond defaults are brittle. | Pass obs-key config through fine-tune factory. |
| Fine-tune env rollouts require single local device | Medium/High | Assertion: `local_devices_to_use==1` (`world_model/finetune_wm_ac.py:1450`). | Blocks multi-GPU fine-tuning. | Refactor env buffer collection and pmap layout before multi-GPU use. |
| CPU-GPU round trips in eval/render/checkpoint | Medium | Eval metrics use NumPy mean (`brax_env/brax_env_utils.py:132`); rendering uses host MuJoCo renderer (`mujoco_playground/mujoco_playground/_src/mjx_env.py:315`, `mujoco_playground/mujoco_playground/_src/mjx_env.py:323`); checkpoints use dill (`train_in_mujoco_playground.py:414`). | Throughput measurements may include host overhead; rendering is not device-resident. | Disable render/log-heavy features for benchmark runs; isolate eval cadence. |
| RSL-RL wrapper optional dependency | Medium | `rsl_rl` import falls back to `object` (`mujoco_playground/mujoco_playground/_src/wrapper_torch.py:23`, `mujoco_playground/mujoco_playground/_src/wrapper_torch.py:26`). | Wrapper may instantiate but not behave as expected without dependency. | Explicitly require/check `rsl_rl` before using Torch/RSL path. |
| License/asset redistribution UNKNOWN | Medium | Code packages declare Apache 2.0 (`setup.py:15`, `mujoco_playground/README.md:3`, `brax_env/setup.py:30`), but binary robot assets were not audited. | Redistribution of meshes/checkpoints may have separate terms. | Audit asset provenance before publication or redistribution. |

